apply plugin: 'java'

jar {
    manifest {
        attributes (
                'Implementation-Title': 'Gradle Quickstart',
                'Implementation-Version': '1.1',
                'Main-Class': 'main.java.Main'
        )
    }
}

repositories {
    mavenCentral()
}